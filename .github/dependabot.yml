version: 2
updates:
  # npm dependencies (application + dev)
  - package-ecosystem: npm
    directory: "/"
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 5
    commit-message:
      prefix: "chore(deps)"
      include: scope
    groups:
      types:
        patterns:
          - "@types/*"
      # Keep the entire lint/format toolchain in one PR so peer deps
      # (e.g. @eslint/js ↔ eslint ↔ typescript-eslint) stay compatible.
      lint:
        patterns:
          - "eslint"
          - "@eslint/*"
          - "typescript-eslint"
          - "eslint-config-prettier"
          - "prettier"
    labels:
      - dependencies
      - javascript

  # GitHub Actions versions
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
      day: monday
    commit-message:
      prefix: "chore(ci)"
      include: scope
    labels:
      - dependencies
      - github-actions
